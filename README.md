📈 Crypto Price Monitor & Arbitrage API
📖 Описание
Сервис для мониторинга криптовалютных цен и поиска арбитражных возможностей. Автоматически собирает актуальные цены с трёх крупных бирж (Binance, Kraken, Bybit), сохраняет их в PostgreSQL и предоставляет REST API для анализа.

⚙️ Как работает
Парсинг данных
В отдельном потоке запускается бесконечный цикл, который каждые 30 секунд делает запросы к API бирж по списку валют.
Данные обрабатываются через конвертеры под формат каждой биржи и сохраняются в БД с меткой времени.

Структура данных
В БД создаётся таблица price с полями:

site — биржа

currency — валютная пара (например BTCUSDT)

price — текущая цена

date_price — время получения данных

API-эндпоинты

GET /api/v1/prices — получить все сохранённые записи

GET /api/v1/arbitrage/{symbol} — получить последние цены с каждой биржи для указанной валюты (удобно для поиска разницы цен)

Запуск
Парсер стартует в фоновом потоке, одновременно запускается FastAPI-сервер.

✨ Возможности
Автоматический сбор цен с Binance, Kraken, Bybit

Поддержка нескольких валютных пар (BTC, ETH, SOL, ADA, DOGE, XRP, DOT, LTC, MATIC, BNB)

Гибкая архитектура конвертеров под разные API

Сохранение исторических данных в PostgreSQL

REST API для получения цен и поиска арбитража

Многопоточность: парсер и API работают одновременно

Готов к развёртыванию и масштабированию

🛠 Технологии
Python 3

FastAPI — веб-фреймворк для API

SQLAlchemy — работа с базой данных

PostgreSQL — хранение цен

Requests — HTTP-запросы к API бирж

Threading — параллельный запуск парсера и сервера

Uvicorn — ASGI-сервер для запуска FastAPI

Datetime — метки времени для каждой записи
